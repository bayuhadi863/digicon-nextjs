'use client';

import React from 'react';
// mantine import
import { Pagination } from '@mantine/core';
// next js import
import { useRouter } from 'next/navigation';

const TopicPagination = ({ questionsLength, pageNumber }: { questionsLength: number; pageNumber: number }) => {
  return <div>TopicPagination</div>;
};

export default TopicPagination;
